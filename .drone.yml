kind: pipeline
type: docker
name: sonar_analysis

steps:
  - name: sonar_scan
    image: maven:3.8.6-openjdk-17  # Imagen de Maven con JDK 17
    working_dir: Hotel/backend/sistema-hoteles-be  # Directorio donde está el proyecto
    commands:
      - mvn clean verify sonar:sonar \
        -Dsonar.projectKey=DRONEARQ \
        -Dsonar.projectName='DRONEARQ' \
        -Dsonar.host.url=https://0zvgrx6x-9000.use.devtunnels.ms \
        -Dsonar.token=sqp_dd09130614766a06193ca48af6a7ac953caa1fc8